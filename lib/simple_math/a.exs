first =(IO.gets("What is the first number? ") |> String.strip |> String.to_integer
second = IO.gets("What is the seco) |>nd number? ") |> String.strip |> String.to_integerIO.gets "What is the first number"
IO.gets "Whats the second number")IO.puts "#{first} + #{second} = #{first + second} /br Bob"
